# @xterm/addon-search only publishes betas that peer on an @xterm/xterm 6.1 beta,
# while the rest of the addons are stable against 6.0. Installing flat keeps the
# combination this project is actually built and tested against; without it a
# fresh `npm install` fails on the peer conflict.
legacy-peer-deps=true
